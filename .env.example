# ──────────────────────────────────────────────────────
# Berufsmesse – Umgebungsvariablen
# Kopiere diese Datei zu ".env" und passe die Werte an.
# Die .env-Datei wird NICHT in Git eingecheckt.
# ──────────────────────────────────────────────────────

# ── Datenbank ─────────────────────────────────────────
# MariaDB-Root-Passwort (nur intern vom db-Container genutzt)
DB_ROOT_PASS=bitte_sicheres_root_passwort_setzen

# Anwendungs-Datenbankbenutzer
DB_USER=berufsmesse
DB_PASS=bitte_sicheres_passwort_setzen
DB_NAME=berufsmesse

# ── Anwendung ─────────────────────────────────────────
# Port, unter dem die App erreichbar ist (Standard: 9000)
APP_PORT=9000

# Basis-URL der Anwendung (mit abschließendem /)
# Beispiel für Unterverzeichnis: /berufsmesse/
BASE_URL=/

# ── phpMyAdmin (optional, nur mit --profile tools) ────
# Port für phpMyAdmin (Standard: 8080)
PMA_PORT=8080
